// Client-side Supabase client — usar solo en 'use client' components
export { createClient as createBrowserClient } from './client';

// Server-side Supabase client — usar solo en Server Components / Route Handlers
// Importar directamente: import { createClient } from '@/lib/supabase/server'
